[project]
name = "federated"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "SohaKhare", email = "sohaajaykhare2006@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "catboost>=1.2,<2.0",
    "flwr[simulation]>=1.34,<2.0",
    "joblib>=1.3,<2.0",
    "pandas>=2.2,<3.0",
    "pillow>=11.0,<12.0",
    "scikit-learn>=1.9.0",
    "torch>=2.7,<3.0",
    "xgboost>=2.0,<3.0",
]

[project.scripts]
federated = "federated:main"
train-disease-model = "federated.disease_model:main"
prepare-data = "federated.prepare_data:main"
prepare-xray-data = "federated.prepare_xray_data:main"
run-federated = "federated.run:main"
federated-service = "federated.service:main"
run-xray-federated = "federated.xray_run:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.flwr.app]
publisher = "local"

[tool.flwr.app.components]
serverapp = "federated.server_app:app"
clientapp = "federated.client_app:app"

[tool.flwr.app.config]
num-server-rounds = 1
local-epochs = 3
batch-size = 32
learning-rate = 0.01

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cpu" }
